__pycache__
**/__pycache__
*.py[cod]
.cache
.git
.github
.venv
.env
.env.*
*.env
.pytest_cache
.playwright-cli
output
data
logs
server_zwischenstand
.test-tmp*
.docker-build-session*.log
htmlcov
.coverage
pytest.ini
# AP-SRV-070 W4C: dist/voicestt/*.whl and dist/kroko/*.whl are the production
# Dockerfile's deliberate build inputs (see tools/build_production.py) and
# must reach the build context; only their own past sdist/build scratch
# output would need excluding, and neither Dockerfile COPYs anything else
# from dist/.
build
*.egg-info
.dockerignore
docker-compose.yml
Dockerfile
