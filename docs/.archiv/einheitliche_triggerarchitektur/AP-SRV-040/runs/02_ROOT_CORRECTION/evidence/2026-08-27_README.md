# AP-SRV-040 C2 – Testnachweise

Umgebung: Windows 11, Python 3.12.10, pytest 9.1.1, kein neues venv.
Worktree `workspaces/ap-srv-040`, Branch `review/AP-SRV-040/run-01`.

## stateVersion-Testmatrix

Alle zehn von Root geforderten Pflichtfälle, in
`tests/unit/test_protocol_v2_state_version.py`:

| # | Pflichtfall | Test | Ergebnis |
|---:|---|---|---|
| 1 | `trigger_suppression` applied: `N → N+1` in Ack und Snapshot, `suppressed`/`effective` wirklich geändert | `test_an_applied_suppression_advances_ack_and_snapshot` | pass |
| 2 | `trigger_suppression` `no_change`: keine Erhöhung | `test_a_no_change_suppression_advances_nothing` | pass |
| 3 | `trigger_suppression` Replay: identisches Ack inkl. `stateVersion`, keine weitere Erhöhung | `test_a_suppression_replay_returns_the_original_version` (20x) | pass |
| 4 | `audioAvailable true → false` in `idle`: Version steigt, Ack und Snapshot stimmen überein | `test_losing_the_device_in_idle_advances_the_version` | pass |
| 5 | Availability `no_change` / Replay: keine zweite Erhöhung | `test_a_repeated_availability_value_advances_nothing`, `test_an_availability_replay_returns_the_original_version` (20x) | pass |
| 6 | `finish` aus `waiting_first_speech`: Ack zeigt `closing_input`, Ack-Version > `activation.started`, `activation.input_closed` nochmals höher | `test_finish_versions_the_closing_entry_and_the_close` | pass |
| 7 | `cancel` analog | `test_cancel_versions_the_closing_entry_and_the_close` | pass |
| 8 | Device-Close ändert Availability und Phase: konsistent, kein Doppel-/Missing-Bump | `test_a_device_close_versions_one_logical_change_once` | pass |
| 9 | `snapshot.request`: Version unverändert | `test_a_snapshot_request_advances_nothing` | pass |
| 10 | Diagnostik erhöht nichts | `test_a_diagnostic_event_never_advances_the_version`, `test_a_refused_trigger_diagnostic_advances_nothing` | pass |

Zusätzlich abgesichert:

* jede Ablehnung (`invalid_payload`, `stale_session`, `stale_activation`,
  `settings_rejected`) lässt die Version unberührt –
  `test_no_refusal_advances_the_version`;
* `command_id_conflict` lässt Version **und** Zustand unberührt –
  `test_a_command_id_conflict_advances_nothing`;
* ein `refresh`, der eine längere Restfrist nicht verschiebt, ist `applied`,
  aber keine sichtbare Änderung – `test_a_no_op_refresh_advances_nothing`;
* ein wiederholtes Control in `closing_input`/`idle` schreibt nichts fort –
  `test_a_repeated_control_in_closing_input_advances_nothing`;
* ein von der Recovery wiederholter Close-Eintritt schreibt nur einmal fort –
  `test_a_retried_close_versions_the_entry_only_once`;
* im Vollzyklus gilt exakt `stateVersion == lastEventSeq + 1` – genau ein
  eventloser Fortschritt (der `closing_input`-Eintritt), also weder ein
  fehlender noch ein doppelter Bump –
  `test_a_full_activation_cycle_never_reuses_or_skips_a_version` (20x);
* `advance_state()` ist unter acht parallelen Threads lückenlos –
  `test_concurrent_advances_never_collide` (20x).

Ordnung ausschließlich über `threading.Barrier`; Timeouts nur als Failsafe.

## Rot-vor-Grün

Die neue Suite gegen den unveränderten C1-Code (drei Quelldateien gestasht):

```text
30 failed, 14 passed
```

Dieselbe Suite gegen C2:

```text
21 passed, 105 subtests passed
```

## Suitenergebnisse

| Suite | Ergebnis |
|---|---|
| `tests/unit/test_protocol_v2_state_version.py` | 21 passed, 105 subtests |
| `tests/unit/test_protocol_v2_contract.py` | 66 passed, 1 skipped, 40 subtests |
| `tests/unit/test_protocol_v2_e2e.py` | 68 passed, 16 subtests |
| `tests/unit/test_protocol_v2_races.py` | 6 passed, 120 subtests |
| AP-SRV-030 Fokus | 144 passed, 79 subtests |
| AP-SRV-020 Regression | 53 passed, 3 subtests |
| Vollsuite `tests/unit` | 776 passed, 14 skipped, 448 subtests |

C1-Vollsuite zum Vergleich: 755 passed, 14 skipped, 343 subtests.
Delta: `+21` Tests, `+105` Subtests. Kein zuvor grüner Test wurde rot.

## Wiederholungsläufe

`test_protocol_v2_state_version.py` und `test_protocol_v2_races.py` gemeinsam,
je Lauf 225 Subtests:

```text
Lauf 1: 27 passed, 225 subtests
Lauf 2: 27 passed, 225 subtests
Lauf 3: 27 passed, 225 subtests
```

`test_protocol_v2_state_version.py` allein zusätzlich 6x einzeln
wiederholt – stabil grün.

`test_protocol_v2_e2e.py` und `test_protocol_v2_contract.py` 8x wiederholt:
je `134 passed, 1 skipped, 56 subtests` – keine Flakes.

## Sonstiges

```powershell
git diff --check
```

```text
(keine Ausgabe)
```

Der C1-Commit wurde nicht amendiert; C2 ist genau ein zusätzlicher lokaler
Commit auf demselben Branch. Kein Push, kein Merge, kein Rebase.
