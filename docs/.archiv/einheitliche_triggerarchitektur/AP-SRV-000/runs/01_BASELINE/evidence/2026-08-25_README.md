# Evidence-Index – AP-SRV-000 / Run 01_BASELINE

Reproduzierbare Nachweise werden hier abgelegt oder mit Befehl, Umgebung und
Fundstelle indexiert. Keine Secrets, Laufzeitmodelle oder großen generierten
Logs committen.

## Umgebung

- Windows / PowerShell
- Python 3.12.10
- pytest 9.1.1
- Start-HEAD `13c162950b944dc715fdd81983a7465f8eb0fd79`
- tracked Baseline-Patch-Hash
  `5215a51ebdca428aded5af61a34da3fb44788fdb`

## Ausgeführte Nachweise

| Nachweis | Befehl | Ergebnis |
| --- | --- | --- |
| geerbte Triggerbaseline | `python -m pytest -q tests/unit/test_recorder_activation_control.py tests/unit/test_server_activation_controller.py tests/unit/test_server_trigger_contract.py tests/unit/test_server_controlled_e2e.py tests/unit/test_browser_client_contract.py` | 111 passed, 16 subtests passed |
| erweiterter Fokus inkl. Queue, Events, Wake und Pre-Roll | siehe vollständige Befehlsliste im Run-Report | 221 passed, 36 subtests passed |
| vollständige Serversuite vor AP-Ergänzungen | `python -m pytest --basetemp .tmp-pytest-ap-srv-000` | 489 passed, 13 skipped |
| abschließende vollständige Serversuite | lokales `TEMP`/`TMP` gesetzt, danach exakt `python -m pytest` | 492 passed, 13 skipped |

Ein erster Lauf von `python -m pytest` erreichte 460 grüne Tests und 13 Skips,
brach aber 29 `tmp_path`-Setups mit `WinError 5` am globalen Pytest-Tempordner
ab. Derselbe Lauf war mit explizitem lokalem `--basetemp` vollständig grün;
der temporäre Ordner wurde danach entfernt. Es wurden keine Modell-, Audio-
oder Kalibrierartefakte erzeugt oder eingecheckt.

## Wake-Evidence-Bewertung

`tests/unit/test_wakeword.py` prüft Backend-/Modellauflösung und
`tests/unit/test_preroll.py` plus
`tests/unit/test_audio_recorder_preroll_integration.py` prüfen die generische
Pre-Roll-Auswahl. Der geerbte OpenWakeWord-Pfad vergleicht den jeweils letzten
Score mit `wake_words_sensitivity`. Ein deterministischer ausgewähltes-Modell-
Scoreverlauf mit Audio, Cooldown/Latch und exakter Wake-zu-Pre-Roll-Grenze fehlt
noch. Damit ist der vorhandene Harness keine Kalibriergrundlage; WW-18/19
bleiben offen, ohne in AP-SRV-000 Schwellenwerte zu erfinden.
