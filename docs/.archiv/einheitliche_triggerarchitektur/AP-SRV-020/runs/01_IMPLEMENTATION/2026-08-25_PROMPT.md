# Originalauftrag – AP-SRV-020 / Run 01_IMPLEMENTATION

Du implementierst ausschließlich AP-SRV-020 im Serverrepository.

## Laufmetadaten

```text
AP-ID: AP-SRV-020
Run-ID: 01_IMPLEMENTATION
Modell/Lane: GPT-5.6 Sol / Server
Repository: P:\GithubRepos\marcosudau-vps\voice-stt-server\workspaces\einheitliche-triggerarchitektur
Branch: feat/einheitliche-triggerarchitektur
Start-HEAD: 3262079c62c58677cfd6506cd09d020b5b27ef44
Dependency AP-SRV-010: PASS / 3262079c62c58677cfd6506cd09d020b5b27ef44
Planungs-/Wiedereinstiegs-SHA Client: bddd666592dc2a4f8d7f15e9ca2bc72d15ac6ed4
Zielremote nach Root-PASS: github/feat/einheitliche-triggerarchitektur
Push durch Agent: nein
```

Du bist nicht allein im Gesamtprojekt. Setze keine Änderungen anderer zurück
und ändere keine Datei im Clientrepository. Das Clientrepository darf nur zum
Lesen der ausdrücklich genannten kanonischen Planung verwendet werden.

## Verbindliche Lektüre

1. `AGENTS.md` und `docs/.archiv/README.md` im Serverrepository vollständig.
2. Diese AP-Akte: `2026-08-25_README.md` und `2026-08-25_PLAN.md`.
3. Die AP-SRV-010-Berichte und Root-Abnahme, besonders State-Machine-, Gate-
   und Testabdeckungsgrenzen:
   - `AP-SRV-010/runs/01_IMPLEMENTATION/2026-08-25_REPORT.md`;
   - `AP-SRV-010/runs/02_CORRECTION/2026-08-25_REPORT.md`;
   - `AP-SRV-010/runs/01_IMPLEMENTATION/2026-08-25_ABNAHME.md`.
4. Im Client-Planungsrepository
   `P:\GithubRepos\marcosudau-vps\voice-stt-client\workspaces\einheitliche-triggerarchitektur\ARBEITSDATEIEN\10_AKTUELL\EINHEITLICHE_TRIGGERARCHITEKTUR`,
   ausschließlich außerhalb von `IDEEN/`:
   - `NACHVERFOLGUNG/WIEDEREINSTIEG.md`;
   - `PLANUNG/IMPLEMENTIERUNGSPLAN.md`, Abschnitt `AP-SRV-020`;
   - `PLANUNG/TECHNISCHER_CONTRACT_FREEZE.md`, besonders §§ 2.1, 3, 3.1,
     3.2, 5 und 11;
   - `PLANUNG/ZIELBILD.md`, relevante Segment-/Final-/Drainpassagen;
   - `NACHVERFOLGUNG/TRACEABILITY.md`, insbesondere `CORE-10–11`,
     `CORE-13–17`, `HK-03`, `PHASE-02`, `PHASE-04`, `LEDGER-01–08`,
     `TIME-08`, `SAFE-02` und `SAFE-03`.
5. Die tatsächlich betroffenen Servermodule, Scheduler-/Recorderpfade,
   direkten Abhängigkeiten und Tests vollständig.

Lies keinerlei Datei unter `IDEEN/` und insbesondere keine Namespace-Datei.

## Auftrag

1. Verifiziere Branch, exaktes Start-HEAD und Working Tree. Vor deiner ersten
   Produktänderung ist nur die von Root angelegte unversionierte AP-SRV-020-
   Akte zulässig. Führe die relevante Baselinesuite aus.
2. Inventarisiere alle Stellen, an denen heute Finaljobs oder Ergebnisse über
   `generation`, `segment_state.current()`, eine FIFO-Deque oder einen
   globalen Current-Activation-Zeiger korreliert werden, sowie alle Drop-,
   Queue-Trim-, Cancel-, Session-Close- und Workerfehlerpfade.
3. Führe ein klar abgegrenztes, thread-sicheres Ledger-Domainmodell ein. Ein
   angenommener Segment-/Finaljobkontext ist danach unveränderlich und trägt
   mindestens:
   - `sessionId`;
   - `activationId` und `activationSequence`;
   - `segmentId` und sessionsweit streng steigende `segmentSequence`;
   - den unveränderlichen Activation-Settings-Snapshot;
   - die für Korrelation und Exactly-once erforderliche Job-/Request-ID.
   Nutze eine eigene kleine Modulgrenze, wenn dadurch die bereits große
   `server.py` verständlicher bleibt.
4. Ersetze für Finaljobs jede fachliche Korrelation über den jeweils aktuellen
   Vordergrund. Ein verspätetes Ergebnis der Activation A muss A/Segment A
   behalten, auch wenn B bereits offen ist oder drainiert.
5. Implementiere ein Pending-Activation-/Segmentledger mit genau einem
   Terminal pro angenommenem Segment:
   - `completed` nach definitiver Veröffentlichung;
   - `discarded` für bewusst verworfene, technisch nicht als Nutztext
     publizierte Ergebnisse, jeweils mit Grund, darunter `empty_final`;
   - `cancelled` für Cancel-/Sessionabbruchgrenzen;
   - `failed` für Scheduler-/Worker-/technische Fehler.
   Doppelte, stale oder konkurrierende Abschlussversuche dürfen niemals ein
   zweites Terminal oder eine zweite Textpublikation erzeugen.
6. Integriere jeden bestehenden Verlustpfad: Queue-Trim, Queue-Reject/
   Overload, Empty-Final, Scheduler-Drop, Workerexception, Cancel,
   Session-Close und Recovery. Kein Audio-/Jobverlust darf ohne sichtbaren
   maschinenlesbaren Terminalgrund erfolgen. Verwirf bereits veröffentlichten
   Text niemals nachträglich.
7. Implementiere sessionsweite geordnete Nutzresultatpublikation nach
   `segmentSequence`. Out-of-order fertige Resultate warten; ein
   `discarded`-/`cancelled`-/`failed`-Terminal füllt die Sequenzstelle und
   gibt spätere publishbare Resultate frei. Vermeide unbegrenztes Warten durch
   vollständige Terminalisierung aller bekannten Fehler-/Closepfade.
8. Erzeuge je Activation genau ein terminales Activation-Ereignis erst dann,
   wenn Input geschlossen und jedes angenommene Segment terminal ist. Die
   Invariante `acceptedSegmentCount == terminalSegmentCount` muss in Tests und
   beobachtbarem Ledgerzustand nachweisbar sein. Nimm das endgültige
   öffentliche v2-Event-/Snapshotformat aus SRV-040 nicht vorweg.
9. Erhalte Live-Transkription und Early-Final: Early-Inferenz darf während
   Aufnahme laufen, definitive Nutzpublikation erfolgt aber nur über den
   stabilen Segmentkontext. Follow-up und Finalarbeit dürfen parallel laufen;
   Vordergrund-`idle` darf mit älteren drainenden Activations koexistieren.
10. Halte Paketgrenzen strikt:
    - keine neue Command-/Refresh-/Watchdog-/Closing-Recovery-Timerpolitik
      (SRV-030);
    - kein v2-Handshake, endgültiges Wireevent oder v2-Snapshot (SRV-040);
    - keine Settings-Control-Plane, Wake-Kalibrierung oder Legacyentfernung;
    - keine Client- oder Browseränderung.
11. Ergänze deterministische Unit- und E2E-Tests mindestens für:
    - mehrere Segmente einer Activation und mehrere überlappende Activations;
    - absichtlich Out-of-order abgeschlossene Finals;
    - Hole-Fill durch Empty/Discard/Cancel/Failed;
    - Queue-Overflow/-Trim und Scheduler-Reject;
    - Workerexception und Session-Close mit vollständiger Cardinality;
    - doppelten Resultcallback und konkurrierende Terminalversuche;
    - Publish-then-Cancel ohne Rücknahme;
    - alte Finalresultate bei neuer aktueller Activation;
    - kein dauerhaft pendinges Ledger nach allen Abschlusswegen.
12. Bewahre weiterhin gültige vorhandene Testabdeckung. Wenn Tests wegen
    überholter Semantik ersetzt oder entfernt werden, ordne jeden betroffenen
    Test im Report exakt einem neuen Test oder einem begründet entfallenen
    Alt-Soll zu. Testzahlreduktion ohne Abdeckungsnachweis ist nicht
    abnahmefähig.
13. Aktualisiere `docs/einheitliche-triggerarchitektur.md` und alle direkt
    betroffenen dauerhaft gültigen Serverdokumente. Historische datierte
    Planungs-/Prüfdateien bleiben unverändert.
14. Führe fokussierte Ledger-/Scheduler-/Recorder-/E2E-Tests und danach
    `python -m pytest` aus. Nutze beim bekannten Windows-Temp-Rechteproblem
    repositorylokale `TEMP`/`TMP`. Debugge iterativ bis alle relevanten Tests
    grün sind.
15. Fülle `runs/01_IMPLEMENTATION/2026-08-25_REPORT.md` vollständig mit
    Soll/Ist, Dateiliste, Korrelation-vorher/nachher, Terminalmatrix,
    Contractzuordnung, Testabdeckungszuordnung, Testergebnissen, Risiken und
    bewusst verschobenen Folgearbeiten. Aktualisiere den Evidence-Index.
    Ändere weder diesen Originalprompt noch `2026-08-25_ABNAHME.md`.
16. Prüfe den vollständigen Range ab Start-HEAD mit `git diff --check`. Stage
    ausschließlich AP-SRV-020 und erstelle genau einen lokalen Commit:
    `feat(trigger): implement AP-SRV-020 segment ledger`. Nicht pushen.
    Prüfe danach sauberen Working Tree, genau einen Commit seit Start-HEAD und
    den vollständigen Range erneut.

Bei einem echten Blocker: keinen Commit erstellen, reproduzierbaren Nachweis
dokumentieren und `BLOCKED` berichten.

## Pflichtvalidierung

- Ledger-Domainmodell und Terminal-Cardinality;
- Out-of-order-/Hole-Fill-Reihenfolge;
- Multi-Activation-/Multi-Segment-E2E;
- Queue-/Scheduler-/Worker-/Cancel-/Session-Close-Faultmatrix;
- Early-Final-/Follow-up-Parallelität und alte-Final/neue-Activation-Race;
- alle direkt betroffenen bestehenden Tests;
- vollständige Serversuite: `python -m pytest`;
- `git diff --check` über den vollständigen AP-Range;
- `git status --short` und genau ein lokaler Commit.

## Rückgabeformat

```text
Status: PASS | FAIL | BLOCKED
AP / Run:
Start-HEAD:
Lokale Commit-SHA:
Implementiert:
Geänderte Produktdokumentation:
Terminal-/Korrelationmatrix:
Tests mit Ergebnis:
Contract-/Traceability-Zuordnung:
Bewusst verschobene Folgearbeiten:
Abweichungen, Flakes, Risiken:
Working-Tree-Status:
Hinweise für Root-Endabnahme:
```
