# Evidence-Index – AP-SRV-030

Claude trägt hier reproduzierbare Testbefehle, Ergebnisse und notwendige
Nachweise ein. Keine Laufzeitlogs, Audiodaten, Modelle oder Secrets committen.

---

## Umgebung

- Repository-Worktree:
  `P:\GithubRepos\marcosudau-vps\voice-stt-server\workspaces\einheitliche-triggerarchitektur-distributed`
- Branch: `feat/einheitliche-triggerarchitektur-distributed`
- Start-SHA: `8535ee79bb2d898d9897e91b57d6a735c479edf0`
- Python: vorhandene Serverumgebung, `Python 3.12.10`
  (`C:\Users\marco\AppData\Local\Programs\Python\Python312\python.exe`),
  kein neues venv angelegt.
- Windows-Pytest-Tempproblem: `TEMP`/`TMP` und `--basetemp` zeigen auf das
  repositorylokal bereits ignorierte Verzeichnis `.pytest-tmp/`
  (`.gitignore`-Eintrag `.pytest-tmp/`). Es wird nicht committet.

## Reproduzierbare Befehle

Alle Befehle wurden aus dem Worktree-Root ausgeführt. `<WT>` steht für den
oben genannten Worktreepfad.

### 1. Baseline vor jeder Produktänderung

```powershell
$env:TEMP="<WT>\.pytest-tmp"; $env:TMP="<WT>\.pytest-tmp"
python -m pytest --basetemp="<WT>\.pytest-tmp\pt" -q
```

Ergebnis auf unverändertem Start-SHA:
`507 passed, 13 skipped, 103 subtests passed in 35.47s`.

Das entspricht exakt dem in `WIEDEREINSTIEG.md` dokumentierten
AP-SRV-020-Stand (`507 passed, 13 skipped`).

### 2. Fokussierte Suite dieses Pakets

```powershell
python -m pytest --basetemp="<WT>\.pytest-tmp\pt" -q -p no:warnings `
  tests/unit/test_server_activation_controller.py `
  tests/unit/test_server_activation_commands.py `
  tests/unit/test_server_command_timer_e2e.py `
  tests/unit/test_server_trigger_contract.py `
  tests/unit/test_server_controlled_e2e.py `
  tests/unit/test_server_segment_ledger.py
```

Ergebnis: `149 passed, 67 subtests passed in 14.87s`.

Einzeln:

| Datei | Ergebnis |
| --- | --- |
| `tests/unit/test_server_activation_controller.py` | `52 passed, 23 subtests passed` |
| `tests/unit/test_server_activation_commands.py` | `13 passed, 15 subtests passed` |
| `tests/unit/test_server_command_timer_e2e.py` | `27 passed, 26 subtests passed` |
| `tests/unit/test_server_trigger_contract.py` | `18 passed` |

### 3. AP-SRV-020-Regressionssuite

```powershell
python -m pytest --basetemp="<WT>\.pytest-tmp\pt" -q -p no:warnings `
  tests/unit/test_server_segment_ledger.py `
  tests/unit/test_server_controlled_e2e.py
```

Ergebnis: `39 passed, 3 subtests passed in 5.85s`.

### 4. Vollständige Serversuite

```powershell
python -m pytest --basetemp="<WT>\.pytest-tmp\pt" -q
```

Ergebnis nach Abschluss der Implementierung:
`567 passed, 13 skipped, 152 subtests passed in ~33 s`.

### 5. Flake-Prüfung

Die Vollsuite wurde nach Abschluss viermal hintereinander ausgeführt
(33.68 s / 34.07 s / 36.12 s / 33.15 s), die fokussierte Paketsuite fünfmal
(15.75 s – 16.92 s). Alle Läufe lieferten identische Zahlen, keinen
Fehlschlag und keine beobachtete Reihenfolgeabhängigkeit. Es wurde **kein**
Flake reproduziert.

### 6. Diff- und Statusprüfungen

```powershell
git diff --check 8535ee79bb2d898d9897e91b57d6a735c479edf0
git status --short
```

`git diff --check` über den Arbeitsbaum ist sauber. Über den vollständigen
Range `8535ee7..HEAD` meldet es genau einen Hinweis:

```text
docs/.archiv/einheitliche_triggerarchitektur/AP-SRV-030/runs/01_IMPLEMENTATION/2026-08-25_PROMPT.md:848: new blank line at EOF.
```

Er betrifft ausschließlich die byteidentisch übernommene Originaldatei der
Koordination (sie endet auf `\n\n`) und wurde bewusst nicht korrigiert, damit
der Originalprompt verbatim bleibt. Kein Produkt-, Test- oder
Dokumentationsfile erzeugt einen Whitespace-Befund. Die Meldung
`warning: in the working copy of '…', LF will be replaced by CRLF` ist die
normale `* text=auto`-Normalisierung aus `.gitattributes` und kein Befund;
der Diff-Stat zeigt reine Inhaltsänderungen, keinen Zeilenendenumbruch über
ganze Dateien.

## Nachweiszuordnung der Contract-IDs

| Contract-ID | Nachweis |
| --- | --- |
| A-01 | `test_canonical_foreground_uses_exactly_five_phase_values` plus die vollständige Phasenmatrix |
| A-04 | `CommandReplayEndToEndTests`, `CommandReplayCacheTests`, `StaleActivationEndToEndTests` |
| CORE-12 / DEVICE-03 / DEVICE-05 | `AudioAvailabilityEndToEndTests`, `test_audio_loss_cancels_the_open_activation_in_every_phase`, `test_a_reconnect_does_not_revive_the_previous_activation` |
| HK-02 / TIME-02 / TIME-03 | `test_followup_refresh_restarts_the_window_and_never_accumulates`, `test_a_refresh_in_followup_wait_resets_the_deadline_from_now` |
| HK-03 / CMD-04 | `CommandPhaseMatrixTests`, `test_the_closing_input_row_of_the_phase_matrix` |
| SAFE-01 / TIME-04 | `test_segment_start_arms_the_ten_minute_watchdog`, `test_the_watchdog_warns_then_closes_and_still_delivers_the_audio` |
| PHASE-03 | `test_refresh_is_invalid_while_waiting_for_the_first_speech`, `CommandPhaseMatrixTests` |
| PHASE-04 | `test_finish_closes_gate_and_recorder_before_published_idle` (AP-SRV-010, unverändert grün) |
| PHASE-05 | `test_closing_recovery_reaches_idle_and_keeps_the_close_reason`, `ClosingRecoveryEndToEndTests` |
| TIME-01 | `test_initial_speech_timeout_enters_close_barrier`, `test_the_server_closes_the_gate_on_its_own_after_the_timeout` |
| TIME-05 | `test_an_early_watchdog_refresh_never_shortens_the_remaining_time`, `test_a_late_watchdog_refresh_secures_at_least_three_minutes`, `test_repeated_watchdog_refreshes_do_not_accumulate` |
| TIME-06 | `test_vad_activity_does_not_reset_the_watchdog` |
| TIME-07 | `test_every_effective_refresh_raises_the_timer_revision`, `test_only_the_timer_revision_has_to_differ_for_a_callback_to_be_inert`, `test_a_timer_of_a_superseded_deadline_is_inert`, `test_a_watchdog_timer_of_an_ended_segment_is_inert`, `test_a_recovery_timer_cannot_touch_a_newer_activation`, `test_a_stale_recovery_timer_cannot_end_a_newer_activation`, `test_wallclock_jumps_do_not_change_monotonic_deadline_or_expiry` |
| TIME-08 | `test_the_watchdog_warns_then_closes_and_still_delivers_the_audio`, `test_the_watchdog_opens_no_followup_window`, `test_only_one_warning_is_emitted_per_segment_deadline` |
| CMD-02 / CMD-07 | `test_a_control_command_with_a_stale_activation_id_has_no_effect`, `StaleActivationEndToEndTests`, `test_a_stale_command_racing_a_new_activation_is_deterministic` |
| CMD-03 | `CommandPhaseMatrixTests`, `test_new_activation_is_locked_in_every_non_idle_phase` |
| CMD-05 | `test_a_triple_replay_of_activate_opens_exactly_one_activation`, `test_a_refresh_replay_does_not_move_the_deadline_twice`, `test_a_finish_replay_produces_no_second_lifecycle_or_ledger_event`, `test_a_cancel_replay_cancels_exactly_once` |
| CMD-06 | `test_the_same_command_id_with_another_payload_is_a_conflict`, `test_a_different_payload_is_a_conflict` |
| SAFE-02 / LEDGER-03 | `test_recovery_keeps_the_ledger_cardinality_consistent` |
| FEED-03 | `test_the_watchdog_warns_then_closes_and_still_delivers_the_audio` (Timeline-Event `watchdog_warning`) |
| HK-04 (Negativprüfung) | `test_the_additive_extend_semantics_is_gone_from_the_controller` |
| AP-SRV-020-Regression | `LedgerRegressionEndToEndTests`, `BackgroundDrainOverlapTests`, `tests/unit/test_server_segment_ledger.py` |

## Bewusst nicht abgelegte Artefakte

- Keine Laufzeitlogs, keine Audiodateien, keine Modelle, keine Secrets.
- `.pytest-tmp/` ist ignoriert und nicht Teil des Commits.
- `2026-08-25_ABNAHME.md` wurde bewusst **nicht** angelegt; die Datei gehört
  ausschließlich der späteren unabhängigen Abnahme.
