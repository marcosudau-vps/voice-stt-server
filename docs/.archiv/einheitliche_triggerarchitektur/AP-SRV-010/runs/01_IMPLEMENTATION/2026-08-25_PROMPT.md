# Originalauftrag – AP-SRV-010 / Run 01_IMPLEMENTATION

Du implementierst ausschließlich AP-SRV-010 im Serverrepository.

## Laufmetadaten

```text
AP-ID: AP-SRV-010
Run-ID: 01_IMPLEMENTATION
Modell/Lane: GPT-5.6 Sol / Server
Repository: P:\GithubRepos\marcosudau-vps\voice-stt-server\workspaces\einheitliche-triggerarchitektur
Branch: feat/einheitliche-triggerarchitektur
Start-HEAD: 71a35e074eb90d75f8f91f5ed7cb46accd4b6498
Dependency AP-SRV-000: PASS / 71a35e074eb90d75f8f91f5ed7cb46accd4b6498
Planungs-/Gate-SHA Client: eddc4c4a60fdfd2ca9fd61520436939fb6de1305
Zielremote nach Root-PASS: github/feat/einheitliche-triggerarchitektur
Push durch Agent: nein
```

Du bist nicht allein im Gesamtprojekt. Setze keine Änderungen anderer zurück
und ändere keine Datei im Clientrepository. Das Clientrepository darf nur zum
Lesen der ausdrücklich genannten kanonischen Planung verwendet werden.

## Verbindliche Lektüre

1. `AGENTS.md` und `docs/.archiv/README.md` im Serverrepository vollständig.
2. Diese AP-Akte: `2026-08-25_README.md` und `2026-08-25_PLAN.md`.
3. Baselinebericht:
   `docs/.archiv/einheitliche_triggerarchitektur/AP-SRV-000/runs/01_BASELINE/2026-08-25_REPORT.md`.
4. Im Client-Planungsrepository
   `P:\GithubRepos\marcosudau-vps\voice-stt-client\workspaces\einheitliche-triggerarchitektur\ARBEITSDATEIEN\10_AKTUELL\EINHEITLICHE_TRIGGERARCHITEKTUR`,
   ausschließlich außerhalb von `IDEEN/`:
   - `PLANUNG/IMPLEMENTIERUNGSPLAN.md`, Abschnitt `AP-SRV-010`;
   - `PLANUNG/TECHNISCHER_CONTRACT_FREEZE.md`, besonders §§ 3–5 sowie
     Settings-Snapshot und Recoverygrenzen;
   - `PLANUNG/ZIELBILD.md`, relevante Lifecycle-/Activationpassagen;
   - `NACHVERFOLGUNG/TRACEABILITY.md`, insbesondere `CORE-01–04`,
     `CORE-10–11`, `CORE-16`, `A-01`, `PHASE-01–04`, `CMD-03`, `WIRE-12`
     und `SET-07`.
5. Die tatsächlich betroffenen Servermodule, direkten Abhängigkeiten und
   Tests vollständig.

Lies keinerlei Datei unter `IDEEN/` und insbesondere keine Namespace-Datei.

## Auftrag

1. Verifiziere vor Änderungen Branch, exaktes Start-HEAD und sauberen
   Working Tree. Die durch Root angelegte AP-Akte ist der einzige erwartete
   uncommittete Umfang.
2. Analysiere die vorhandenen Controller-, Gate-, Recorder-, Session- und
   Eventpfade. Erhalte wiederverwendbare Synchronisations- und
   Idempotenzmechanismen; refaktoriere nur soweit für den Contract nötig.
3. Führe ein kanonisches Domainmodell ein für:
   - exakt `idle`, `waiting_first_speech`, `segment_active`, `followup_wait`,
     `closing_input` als Vordergrundphasen;
   - stabile `activationId` und sessionsweit steigende
     `activationSequence` je akzeptierter manueller oder serverinterner
     Wake-Word-Admission;
   - genau eine gelatchte First-Trigger-Quelle;
   - einen defensiv kopierten, unveränderlichen Effective-Settings-Snapshot
     beim Activationstart.
4. Entferne Source-Merge vollständig aus der Domainsemantik. Ein weiterer
   Trigger während `waiting_first_speech`, `segment_active`, `followup_wait`
   oder `closing_input` darf Quelle, ID, Sequenz und Settings-Snapshot nicht
   ändern und keine zweite Activation öffnen. Das Ergebnis muss
   deterministisch `activation_locked` ausdrücken. Ändere in diesem AP noch
   nicht das endgültige externe v2-Wireformat aus AP-SRV-040.
5. Binde Gate- und Recorderübergänge so an den Controller, dass:
   - Speech erst nach Admission ein Segment beginnen kann;
   - Recording-Start zu `segment_active` führt;
   - Recording-Ende bei offenem Eingabefenster zu `followup_wait` führt;
   - mehrere serielle Segmente dieselbe Activation behalten;
   - der sichere Eingabeschluss über `closing_input` nach `idle` führt;
   - Gate/Recorder vor veröffentlichtem `idle` geschlossen sind.
6. Entferne `finalizing` als Vordergrundphase und Trigger-Lock. Bereits
   laufende Finalarbeit darf im Hintergrund fortbestehen, während der
   Vordergrund wieder `idle` ist und eine neue Activation zulässt. Baue in
   diesem AP noch kein Segmentledger oder Reorder-System aus AP-SRV-020.
7. Trenne die AP-Grenzen strikt:
   - keine Ledger-/Reorder-/Pending-Registry-Implementierung (SRV-020);
   - keine endgültige Command-, Refresh-, Watchdog- oder 5-s-Recovery-
     Timerpolitik (SRV-030). Modell und Exitpfade müssen dafür anschlussfähig
     sein; bestehende Timer dürfen nur minimal an die neuen Phasen angepasst
     werden, damit es keine Regression gibt;
   - kein Protokoll-v2-/Handshake-/Snapshotumbau (SRV-040);
   - keine Settings-Control-Plane, Wake-Kalibrierung oder Legacyentfernung.
8. Ersetze bewusst überholte Baseline-Solltests zu Source-Merge und
   blockierendem `finalizing` durch Zieltests. Ergänze vollständige
   Transition-, Negativ-, Multi-Segment-, Overlap- und wiederholte echte
   Parallel-Race-Tests. Schwäche keine Assertion ab, nur um Tests grün zu
   bekommen.
9. Aktualisiere die dauerhaft gültige Serverdokumentation, insbesondere
   `docs/einheitliche-triggerarchitektur.md`, sowie bei Bedarf direkt
   betroffene Modul-/Protokolldokumentation. Historische Archivdokumente von
   12./14. August bleiben unverändert.
10. Führe fokussierte Tests und danach `python -m pytest` aus. Nutze bei dem
    bekannten Windows-Temp-Rechteproblem einen expliziten lokalen
    `--basetemp` beziehungsweise lokale `TEMP`/`TMP`; lege keine globale
    Umgebung an. Debugge iterativ bis alle relevanten Tests grün sind.
11. Fülle
    `runs/01_IMPLEMENTATION/2026-08-25_REPORT.md` vollständig mit Soll/Ist,
    Dateiliste, Contractzuordnung, Tests, Risiken und bewusst verschobenen
    Folgearbeiten. Aktualisiere den Evidence-Index. Ändere weder diesen
    Originalprompt noch `2026-08-25_ABNAHME.md`.
12. Prüfe den vollständigen Range ab Start-HEAD mit `git diff --check
    71a35e074eb90d75f8f91f5ed7cb46accd4b6498..HEAD` beziehungsweise vor dem
    Commit gegen den Working Tree. Stage ausschließlich AP-SRV-010.
13. Erstelle genau einen lokalen Commit mit der Message
    `feat(trigger): implement AP-SRV-010 state machine`. Nicht pushen. Prüfe
    danach sauberen Working Tree, genau einen Commit seit Start-HEAD und den
    vollständigen Range erneut.

Wenn ein echter Blocker verbleibt, erstelle keinen Commit, dokumentiere den
reproduzierbaren Befund und berichte `BLOCKED`.

## Pflichtvalidierung

- Controller-State-Machine- und Negativtests;
- Gate-/Recorder-/Mehrsegmentintegration;
- wiederholte Parallel-Race-Tests für Manual/Wake First-Trigger-wins;
- Overlap: neue Activation bei älterer Hintergrund-Finalisierung;
- alle direkt betroffenen Contract-/E2E-Tests;
- vollständige Serversuite: `python -m pytest`;
- `git diff --check` über den vollständigen AP-Range;
- `git status --short` und genau ein lokaler Commit.

## Rückgabeformat

```text
Status: PASS | FAIL | BLOCKED
AP / Run:
Start-HEAD:
Lokale Commit-SHA:
Implementiert:
Geänderte Produktdokumentation:
Tests mit Ergebnis:
Contract-/Traceability-Zuordnung:
Bewusst verschobene Folgearbeiten:
Abweichungen, Flakes, Risiken:
Working-Tree-Status:
Hinweise für Root-Endabnahme:
```
