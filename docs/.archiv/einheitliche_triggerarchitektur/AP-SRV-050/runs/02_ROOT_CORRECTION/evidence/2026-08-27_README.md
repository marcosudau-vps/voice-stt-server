# AP-SRV-050 – Root-Korrektur C2 – Evidence

**Datum:** 2026-08-27

## Originalprompt (C2)

- Quelle: `P:\GithubRepos\marcosudau-vps\_workflow-tools\AP-SRV-050\Prompts\DEEPSEEK_AP-SRV-050_ROOT_CORRECTION_C2.md`
- Nachweis: `runs/02_ROOT_CORRECTION/2026-08-27_PROMPT.md` (byteidentische Kopie)

| Datei | SHA256 |
|---|---|
| Quelle | `B3B43D43D09ED8353E6A71954BDD16A7AC50BD62F2DE36DEDABDE3B3436607D7` |
| Nachweis | `B3B43D43D09ED8353E6A71954BDD16A7AC50BD62F2DE36DEDABDE3B3436607D7` |

## Testbefehle und exakte Counts

Alle Befehle mit `python -m pytest -q --basetemp=...`; Laufumgebung: Python
3.12.10, pytest 9.1.1. Die WMI-/torch-Mitigation (siehe unten) war aktiv.

### Red→Green-Nachweise (je Finding mind. ein deterministischer Rotlauf auf C1)

- F1 (sequential cross-field bypass): 3 Tests `applied` statt `settings_rejected`
  auf C1 → nach Fix grün (GATE F1 `10 passed`).
- F2 (wake fail-open): 3 Tests ohne Error auf C1 → nach Fix grün
  (GATE F2 `9 passed`).
- F3 (keine atomare Admission): Bundle-Seam auf C1 nicht vorhanden
  (`AttributeError`) → nach Fix grün (GATE F3 `24 passed`; F3-Race isoliert 20× grün).
- F4 (persisted overlay unvalidiert): 8+ Startup-Tests werfen auf C1 nicht
  → nach Fix grün (GATE F4 `34 passed`).
- F5 (Persistenzfehler = halber Commit): RAM-Mutation + Exception/leak auf C1
  → nach Fix grün (GATE F5 `30 passed`).
- F6 (`requestedSettings` fehlt): alle `RequestedSettingsWireTests` rot auf C1
  → nach Fix grün (GATE F6 `61 passed`).

### Zwischen-Gates F1–F6

```text
GATE F1: 10 passed
GATE F2:  9 passed
GATE F3: 24 passed
F3-Race isoliert: 20× single-test-pass (je 20 Iterationen im Test)
GATE F4: 34 passed
GATE F5: 30 passed
GATE F6: 61 passed
```

### Kombinierte Gates

```text
C2-Settings (drei Settings-Dateien):        118 passed
Protocol v2-Regression:                     203 passed, 1 skipped, 281 subtests passed
Activation-/Timer-Regression:                218 passed, 82 subtests passed
Race-Gate 3 Serien (control_plane / wire / persistence):
  Serie 1: 64 / 39 / 15 passed
  Serie 2: 64 / 39 / 15 passed
  Serie 3: 64 / 39 / 15 passed
Full Unit (tests/unit):                     897 passed, 14 skipped, 448 subtests passed
```

### `git diff --check`

```text
clean (kein Output)
```

## WMI-Mitigation

Diese Maschine blockiert im torch-Import `platform._wmi_query()` (CPython 3.12,
`platform.machine()`), wodurch pytest beim Sammeln der Suite hängen würde. Wie in
C1 wird ausschließlich eine **out-of-tree** `sitecustomize`-/`PYTHONPATH`-Mitigation
verwendet
(`C:\Users\marco\AppData\Local\Temp\opencode\wmi-mitigation\sitecustomize.py`,
leitet `platform._wmi_query` auf die dokumentierte Fallback-Methode). Keine
Repository-Datei, keine Änderung an Testbefehlen/-Sollwerten.

## Race-Wiederholungen

- A concurrent same-base session patch: in `test_settings_control_plane`
  (`TestConcurrentSessionPatches`, 20×) und `test_protocol_v2_settings`
  (`test_concurrent_same_base_patches_one_wins_20x`, 20×).
- B settings.changed vs. Domain-Event-Ordering: `SettingsChangedOrderingTests`
  (20 Iterationen je Test).
- C RuntimeConfig-Parallel-Write: `test_settings_runtime_persistence`
  (einzelne und separate Instanzen).
- D F3-Admission-Bundle vs. Settingspatch: `test_admission_bundle_vs_patch_is_atomically_ordered_20x`
  (20× äußerlich + 20 Iterationen innerlich; 20 isolierte Läufe grün).

## Umfangsbestätigung

- Kein `GET /api/v2/wake-words` implementiert.
- Kein AP-SRV-060-/C1-Architekturumbau; `activation.py` unverändert.
- `ROOT_TRACKING_ACTION_REQUIRED:` SET-13 in SET-13a/AP-SRV-050 und
  SET-13b/AP-SRV-060 aufteilen.
- Kein Push.